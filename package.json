{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm run build:uswds && astro build",
    "build:uswds": "gulp copyAssets && gulp compile",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@uswds/uswds": "3.14.0",
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@uswds/compile": "^1.3.2",
    "gulp": "^5.0.1",
    "gulp-cli": "^3.1.0"
  }
}
